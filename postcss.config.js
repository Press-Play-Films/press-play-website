// postcss.config.js
module.exports = {
  plugins: {
    // Tailwind’s PostCSS plugin (moved into its own package):
    '@tailwindcss/postcss': {},
    // Autoprefixer (optional but recommended):
    autoprefixer: {},
  },
}